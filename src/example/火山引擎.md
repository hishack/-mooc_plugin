curl https://ark.cn-beijing.volces.com/api/v3/chat/completions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $ARK_API_KEY" \
  -d '{
    "model": "doubao-seed-1-6-251015",
    "messages": [
        {
            "role": "user",
            "content": "hello"
        }
    ]
  }'
如果返回错误码 {"error":{"code":"AuthenticationError","message":"The API key or AK/SK in the request is missing or invalid. requestId: 0217***，说明 API Key 没有被正确设置到环境变量中，检查您的环境配置或直接替换$ARK_API_KEY为 API Key ，再次运行命令。